sudo: required

services:
  - docker

before_install:
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
  - sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
  - sudo apt-get update
  - sudo apt-get -y install docker-ce
  - sudo service docker restart

script:
  - cd prometheus && docker build -t diabelb/swarmprom-prometheus:$TRAVIS_BUILD_NUMBER .
  - cd .. && cd node-exporter && docker build -t diabelb/swarmprom-node-exporter:$TRAVIS_BUILD_NUMBER .
  - cd .. && cd alertmanager && docker build -t diabelb/swarmprom-alertmanager:$TRAVIS_BUILD_NUMBER .
  - cd .. && cd grafana && docker build -t diabelb/swarmprom-grafana:$TRAVIS_BUILD_NUMBER .

after_success:
  - if [ -z "$DOCKER_USER" ]; then
      echo "PR build, skipping Docker Hub push";
    else
    docker login -u "$DOCKER_USER" -p "$DOCKER_PASS";
    docker tag diabelb/swarmprom-prometheus:$TRAVIS_BUILD_NUMBER diabelb/swarmprom-prometheus:v2.5.0;
    docker push diabelb/swarmprom-prometheus:v2.5.0;
    docker tag diabelb/swarmprom-node-exporter:$TRAVIS_BUILD_NUMBER diabelb/swarmprom-node-exporter:v0.16.0;
    docker push diabelb/swarmprom-node-exporter:v0.16.0;
    docker tag diabelb/swarmprom-alertmanager:$TRAVIS_BUILD_NUMBER diabelb/swarmprom-alertmanager:v0.15.3;
    docker push diabelb/swarmprom-alertmanager:v0.15.3;
    docker tag diabelb/swarmprom-grafana:$TRAVIS_BUILD_NUMBER diabelb/swarmprom-grafana:7.3.10;
    docker push diabelb/swarmprom-grafana:7.3.10;
    fi
